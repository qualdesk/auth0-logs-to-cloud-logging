{
  "name": "auth0-logs-to-cloud-logging",
  "version": "1.0.0",
  "description": "A connector fetching logs from Auth0 and sending them to Google Cloud Logging",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel 'src' --out-dir 'dist' --source-maps",
    "start": "nodemon --exec babel-node ./example.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Qualdesk",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/logging": "^7.3.0",
    "auth0-log-extension-tools": "^1.3.9"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/node": "^7.8.7",
    "@babel/preset-env": "^7.9.0",
    "nodemon": "^2.0.2"
  }
}
